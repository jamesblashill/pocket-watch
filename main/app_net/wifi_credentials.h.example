#pragma once

/* Copy this file to wifi_credentials.h (gitignored) and fill in your
 * network's details. wifi_credentials.h is included directly by
 * wifi_time_sync.c and is never checked into the repo. */

#define WIFI_SSID "your-ssid"
#define WIFI_PASS "your-password"
